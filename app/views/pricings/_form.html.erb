<%= form_for(@pricing) do |f| %>
  <% if @pricing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pricing.errors.count, "error") %> prohibited this pricing from being saved:</h2>

      <ul>
      <% @pricing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :daily_price %><br>
    <%= f.number_field :daily_price %>
  </div>
  <div class="field">
    <%= f.label :monthly_price %><br>
    <%= f.number_field :monthly_price %>
  </div>
  <div class="field">
    <%= f.label :yearly_price %><br>
    <%= f.number_field :yearly_price %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
